import contractAddress from './contractAddress.json';
import contractAbiJson from './CrowdfundingPlatform.json';

export const CONTRACT_ADDRESS = contractAddress.address as `0x${string}`;
export const CONTRACT_ABI = contractAbiJson.abi;
